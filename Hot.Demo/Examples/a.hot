mod main;

# use lib;

let test = fn c, d -> {
	#let a = [];
	#let b = {};
	ret c / d;
};

# 主函数
let main = fn args -> {
	let a = 123.4;
	let b = 123.5;
	let c = test(a,b);
	ret a + b * 1 + 8 * 5 - 9 / 6;
};
