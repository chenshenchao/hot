mod main;

# use lib;

let test = fn c, d -> {
	let a = [];
	let b = {};
	let e = [1,2,3,10.2 + 4];
	let f = {
		key1: 123,
		key2: [1,2,3,4,5],
	};
	ret 1 + c > d - 5 && true;
};

# 主函数
let main = fn args -> {
	let a = 123.4;
	let b = 123.5;
	let c = test(a,b);
	ret a + b * 1 + 8 * 5 - 9 / 6;
};
